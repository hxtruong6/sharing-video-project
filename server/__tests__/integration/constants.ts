const USER_BASE_URL = '/api/user';
const VIDEO_BASE_URL = '/api/video';

const timestamp = () => new Date().getTime();

// eslint-disable-next-line import/prefer-default-export
export { timestamp, USER_BASE_URL, VIDEO_BASE_URL, };
