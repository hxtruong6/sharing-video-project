import Tables, { VideoUserTable } from '../constants/schema';
import db from '../models';
import { toUrlString } from '../utils/commonFuncs';
import { convertSnakeKeys } from '../utils/converts';

class VideoUserService {

}

export default new VideoUserService();
